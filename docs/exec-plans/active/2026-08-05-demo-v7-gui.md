# demo_v7: GUI 软件化 (2026-08-05)

Owner ask (verbatim intent): demo_v6_2 的数据处理流程/标准原样保留(分割、
追踪、shape-prior、chunk、PhysTwin_shen 调用均不许改 —— 分工边界),新开
demo_v7 做成 GUI 软件。明确的流程改动只有:(1) frame-0 由按钮拍摄 + 确认/
重拍;(2) warmup 阶段人离开、无追踪,全部 frame-0 派生计算后台跑并在 GUI
展示进展与结果(mask 检查、shape-prior 渲染/网格、对齐效果);(3) 正式开始
前摆位步骤(mask 半透明叠加,人对齐后按钮开始)。fake-live 与 real 同一套
按钮,fake 播完弹窗回起点。技术栈(Rust vs Python)与 EdgeTAM 在 warmup
是否追踪由我决定。

## 定案

- **PySide6 而非 Rust**:流水线本就是 Python 子进程,Rust 只会把语言边界
  压在最高频改动的接缝(流水线控制)上;GUI 真实负载是 30Hz 贴图(Qt C++
  渲染)。效率纪律:UDS+JPEG、latest-wins、无逐像素 Python 循环。
- **进程拓扑同 6.2**:GUI+orchestrator 父进程(chunk 会话、points.npz
  触发 PhysTwin 复用 6.2);相机服务子进程(CUDA ns 同 6.2 配置)import
  复用 mdp 全部阶段。新增仅:ipc/protocol+channel、按钮驱动状态机
  (staged_runtime)、frame-0 派生管线(frame0_pipeline)、Qt 界面。
- **warmup 无追踪**(委托决定):frame-0 后场景不含摆位物体/手,EdgeTAM/
  tracker 均不启动;正式开始时以摆位后帧为"正式 frame 0",用保存的
  frame-0 SAM3.1 mask 种全新 EdgeTAM session(SegmentationStage 子类只
  覆盖 seeding,其余原码)。这是对 6.2 语义最小且自洽的改动:摆位由人
  对齐 mask 保证 prompt 语义成立。
- 状态机:STARTING→PREVIEW→FRAME0_PENDING→WARMUP→REVIEW→REPOSITION→
  FORMAL→FINISHED(+FATAL)。协议唯一事实源 demo_v7/ipc/protocol.py。
- fake-live 语义修正(owner 2026-08-05):**只 fake 来源,不 fake 相机的
  持续输入特性** —— 流永不暂停(暂停机制整体删除);拍照=抓当前帧快照;
  摆位 overlay 叠实时流;正式 frame-0 = seg 就绪时的当前帧(与 v6.2
  barrier 一致);正式前素材播完自动从头 + 状态栏提示(wrapped=True),
  正式期播完弹窗结束(wrapped=False)。

## 实施

契约先行(README + DESIGN_CONTRACTS.md),5 个并行实现 agent(ipc /
frame0 / service-core / gui / orchestration,文件互斥),集成与修复由主
线完成;随后对抗审查 workflow + 无头端到端(tests/drive_fake_live.py 脚本
化控制通道走完整状态机,downstream disabled,验 2 chunks + points.npz)。
GUI 人工验证与 real-camera 全流程留待有人在桌边时做。

## 验证状态

- 单元:26 v7 tests(ipc 12 + frame0 14)+ v6.2 全量 107 tests 均绿;
  离屏 GUI 构造 + 全状态走查冒烟通过;git 确认 demo_v6_2/qqtt 零改动。
- 端到端(drive_fake_live,downstream disabled):PASS —
  PREVIEW→拍摄→确认→WARMUP(SAM3.1 三 mask + shape-prior 全链,
  ~18s,mesh 缓存命中)→REVIEW(artifact 落盘校验)→摆位→FORMAL
  (EdgeTAM 以保存 mask 种新 session,TAPNext 5000 queries)→4 chunks→
  points.npz→干净 FINISHED/shutdown。
- 端到端过程中抓到并修复的三个真问题:
  1. formal 期 EdgeTAM precompile(~11s)内联在帧流之后 → 采集丢 ~57 帧,
     chunk-0 跨 12s 录制。修复:precompile 提前到 PREVIEW 后台 +
     `_formal_go` 门在 seg 就绪(v6.2 barrier 等价)后才放行 + fake 回放
     REPOSITION 保持暂停、formal-go 才恢复。
  2. cudagraph 线程亲和:提前预编译放独立线程后,formal seg 首 forward
     裸 AssertionError(inductor cudagraph trees 状态绑线程;v6.2 中
     scratch 与正式 forward 同线程)。修复:常驻 seg-host 线程 —— PREVIEW
     时启动、先在该线程做 scratch 预编译、parked 等待 start_formal、
     formal.seg.run() 在同一线程执行。
  3. UDS 路径超长(108 字节 sun_path)被 session 主动校验拒绝(设计正确,
     驱动脚本换短 base path)。
- 虚惊一场(记录以免再犯):chunk-0 object=57/surface=33/interior=1 一度
  疑为 v7 缺陷;逐层排查(行级 visibility 4916/5000、mask 内网格 100%
  有效、frame-0 标注 3950 object、5 帧全门存活 3897)后定位到父进程体素
  认领 —— 而体素 0.05 来自 owner 自己的提交 1b0dd5a(0.005→0.05),
  是当前 v6.2 的预期配置,v7 忠实继承("其他保持一致")。
- 对抗审查(21-agent workflow,保真度/并发/GUI 三维度):18 项发现,
  15 确认 / 3 反驳;确认项全部修复(3 个 fixer agent + 主线集成):
  - MAJOR:GUI 未调用 session.start()(E2E 驱动自己调了 start 所以掩盖;
    真 GUI 不会拉起服务)→ _launch 补 start() + 失败清理;
  - MAJOR:fake-live input_frames.jsonl/input_rgb 时间线从未写入(v6.2
    parity 缺口)→ 三个 formal producer 落盘 write_input_frame,fake 用
    单调 preview seq 保证文件名唯一;
  - MAJOR:FORMAL 期 CMD_SHUTDOWN 跳过 finalize 却报 finished ok →
    shutdown 走 stop_formal 排水 + finalize 后再退出,120s 排水死线
    到期转 fatal 兜底;
  - minor×12:重复 WARMUP_READY 行、frame-0 请求缺 provenance、
    startup_hold_s 语义、ack/state 次序、fake 冻结竞态 + 候选帧钉图
    (EVT_ARTIFACTS frame0 candidate)、channel_max_hz 死配置(经
    --channel-max-hz-json 转发)、控制通道断线重连(服务死亡除外)、
    shutdown 后 PhysTwin 复启竞态闩、closeEvent 阻塞主线程(工作线程化)、
    turntable 视频选择、WarmupScreen.reset 残留、频道上限合并语义。
  - 反驳 3 项(回到开始 10 分钟阻塞等)有据在案。
- 待办:真 GUI 人工验证 + real camera 全流程(需有人在桌边);
  PhysTwin_shen downstream 联动在 GUI 下的首次实跑。

## 追加定案(2026-08-05)

- owner:demo_v7 完全不使用 mesh 缓存,shape prior 每次现场全量生成。
  实现:camera_service 解析后无条件 `args.shape_prior_object = None`
  (v6.2 normalize_object_id 的禁用语义,不读不写缓存),转发内容无关。
  代价:每次 warmup 都付 SAM3D upscale/segment/generate 的现场成本
  (GPU0,分钟级),GUI warmup 时间线本就逐阶段展示。E2E 驱动以真实
  生成复验(不再是缓存命中路径)。
  验证(2026-08-05 深夜):端到端第三跑 PASS —— 现场生成路径首次在 v7 下
  走通,期间修掉两颗 memory 已记录的环境地雷(SAM3D 以 CONDA_PREFIX 为
  CUDA_HOME → session 侧 nvcc 检测 + /usr/local/cuda 回落;torch 2.11
  不注册 JIT 扩展 → 已建 nvdiffrast_plugin 目录挂 PYTHONPATH)。实测:
  object.glb 产自本次运行(mtime 与运行时刻一致),缓存条目 mtime 未变
  (不读不写),确认→REVIEW 全量生成约 68s(缓存命中时代 ~18s),之后
  正式期 3 chunks + points.npz + 干净结束。

## TRELLIS.2 接入(2026-08-05,阶段一:环境/代码/checkpoint)

- owner 指令:shape prior 生成增加选项(sam3d / TRELLIS.2 / None),
  先把 TRELLIS.2 从环境、代码、checkpoint 三方面接入,选择 UI 之后做。
- 落地事实:
  - 代码:`/home/xinjie/TRELLIS.2`(git clone --recursive,main)。
    注意 trellis2 包是 repo-local(不 pip 安装),脚本运行必须
    `PYTHONPATH=/home/xinjie/TRELLIS.2`。
  - 环境:conda env `trellis2`(py3.10,torch 2.6.0+cu124)。与上游
    setup.sh 的偏差:conda 加 -y;跳过 sudo libjpeg-dev + pillow-simd
    (用普通 pillow);系统无 CUDA 12.4(只有 12.8/13.x)→ env 内
    `conda install -c nvidia/label/cuda-12.4.0 cuda-toolkit`,运行时
    CUDA_HOME 指向 env。5 个 CUDA 扩展全部编译通过:flash-attn 2.7.3、
    nvdiffrast v0.4.0、nvdiffrec(renderutils)、CuMesh、FlexGEMM、
    o-voxel。构建脚本(带分步退出码可续跑):scratchpad
    `trellis2_env_build.sh`。
  - **transformers 必须钉 4.57.1**:5.x 把 DINOv3ViTModel 的层挪进内层
    encoder,TRELLIS.2 的 DinoV3FeatureExtractor 直接走 `.layer` 会炸
    (`no attribute 'layer'`)。4.57.1 结构/签名完全吻合。
  - checkpoint:`microsoft/TRELLIS.2-4B`(16G,HF cache);首跑自动补拉
    `facebook/dinov3-vitl16-pretrain-lvd1689m`(不受限,已入 cache)。
  - **rembg(briaai/RMBG-2.0)是 HF gated repo,403 拿不到** —— 但
    `preprocess_image` 对 RGBA 且 alpha 非全 255 的输入根本不调 rembg,
    而我们喂的永远是 SAM3.1 掩码后的 RGBA masked_image → 集成侧在
    from_pretrained 前 stub 掉 `rembg.BiRefNet` 即可,零质量影响。
  - 验证(真实 v7 frame-0 工件 masked_image.png,GPU1):VALIDATION_OK。
    冷加载 53.6s / pipeline.run 90.2s / glb 烘焙导出 40.5s;峰值显存
    仅 2.7GiB(pipeline 默认 low_vram=True 分段上下卡);产物 330k 顶点
    / 489k 面 / 2048² 纹理 glb,几何与纹理肉眼核对与树懒实物一致
    (灰棕绒毛 + 米色脸)。验证脚本:scratchpad `trellis2_validate.py`。
- 集成期注意(阶段二做选择器时):
  - 导出要用 `extension_webp=False`:EXT_texture_webp 的 glb 在 Open3D
    (v7 MeshOrbitView / 任何 o3d 消费方)渲染成纯白;PNG 纹理全兼容,
    代价是文件更大。trimesh/PIL 读 webp 无碍。
  - 生成器进程必须用绝对路径
    `/home/xinjie/miniforge3/envs/trellis2/bin/python`(工程 .venv 的
    py3.12 会影蔽 conda activate 后的 python —— 与 v7 launcher PYBIN
    同款地雷),并带 CUDA_HOME=env、PYTHONPATH=repo。
  - TRELLIS.2 输出坐标在 [-0.5,0.5]³ 归一化 aabb,无真实尺度;对齐链
    (v6.2 align 的 scale 求解)本就处理未知尺度,与 SAM3D glb 同约定。
