# Tech Debt Tracker

- Consider adding a tiny shared CLI utility module if camera entrypoints gain more options.
- Consider adding a dry-run mode to `record_data.py` for operator training without saving files.
- Consider adding a deterministic metadata schema check for recorded raw cases.
